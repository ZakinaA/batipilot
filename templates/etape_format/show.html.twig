{% extends 'base.html.twig' %}

{% block title %}EtapeFormat{% endblock %}

{% block body %}
    <h1>EtapeFormat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape_format.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ etape_format.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etape_format_index') }}">back to list</a>

    <a href="{{ path('app_etape_format_edit', {'id': etape_format.id}) }}">edit</a>

    {{ include('etape_format/_delete_form.html.twig') }}
{% endblock %}
